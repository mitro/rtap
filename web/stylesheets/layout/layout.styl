#app-node,
.___iso-html___
  height: 100%

.l-layout
  display: flex
  flex-direction: column
  align-items: stretch
  justify-content: center
  min-height: 100%

.l-wrapper
  flex: 1 0 auto

.l-container
  position: relative
  margin: 0 auto
  width: $site_width

.l-selectable
  selectable()

.l-not-selectable
  not-selectable()
